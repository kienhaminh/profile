{
  "name": "portfolio-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "db:generate": "turbo run db:generate",
    "db:migrate": "turbo run db:migrate",
    "db:setup": "turbo run db:setup",
    "db:seed-all": "./seed-all.sh"
  },
  "devDependencies": {
    "turbo": "^2.6.0"
  },
  "packageManager": "pnpm@10.18.3"
}
